clc 
clear all
format long
z = 4 + 3 * i; % i or j implies imaginary number
phase_angle = angle(z); %phase angle in radian

angle_in_degree = angle(z) *(180/pi);

imaginary = imag(z);

real = real(z);
complex_conjugate = conj(z);

z1 = 4.9 * -1;
abs(z1)

